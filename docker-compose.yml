services:
  build:
    build: .
    user: "${AC_UID:-1000}:${AC_GID:-1000}"
    volumes:
      - ./events:/app/events
      - ./assets:/app/assets
      - ./config:/app/config
      - ./output:/app/output
    # usage: docker compose run --rm build events/<event>.json
