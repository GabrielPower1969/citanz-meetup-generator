node_modules/
output/*
!output/.gitkeep
.DS_Store

# Real events and speaker photos stay local — only the example is shared
events/*.json
!events/example.json
!events/schema.json
assets/speakers/*
!assets/speakers/scott.png

# Event photos (people) stay local
assets/photos/

# promo video renders (regenerate with node promo/render.mjs)
promo/out/
